# FoodFlow local development defaults. These are not production secrets.
# Do not put real credentials here. Copy to a gitignored file if you need overrides.

FOODFLOW_POSTGRES_USER=foodflow
FOODFLOW_POSTGRES_PASSWORD=foodflow
